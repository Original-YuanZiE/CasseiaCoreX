<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CasseiaCoreX.Pages.Tools"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CasseiaCoreX.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer>
        <Grid Margin="40">
            <StackPanel Orientation="Vertical" Spacing="10" MaxWidth="1000">

                <TextBlock Text="常用工具" FontSize="26" FontWeight="Bold" Margin="0,48,0,38" TextWrapping="Wrap"/>

                <TextBlock Text="驱动管理" FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>
                
                <!--备份驱动-->
                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" MaxWidth="1000" MinHeight="80" Command="{x:Bind ViewModel.BackupDriversCommand, Mode=OneWay}">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE74E;"/>
                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="备份驱动" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Text="将此电脑的所有已安装驱动备份至指定目录" TextWrapping="Wrap"/>
                        </StackPanel>
                        <FontIcon Grid.Column="2" FontFamily="Segoe Fluent Icons" Glyph="&#xE724;"/>
                    </Grid>
                </Button>
                <!--导入驱动-->
                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" MaxWidth="1000" Height="80" Command="{x:Bind ViewModel.ImportDriversCommand, Mode=OneWay}">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8F4;"/>
                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="导入驱动" FontWeight="Bold"/>
                            <TextBlock Text="为此电脑导入驱动"/>
                        </StackPanel>
                        <FontIcon Grid.Column="2" FontFamily="Segoe Fluent Icons" Glyph="&#xE724;"/>
                    </Grid>
                </Button>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
