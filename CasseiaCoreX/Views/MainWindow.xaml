<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CasseiaCoreX.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CasseiaCoreX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CasseiaCoreX">

    <Grid>
        <Grid x:Name="MainGrid">

            <Grid.RowDefinitions>
                <!--标题栏-->
                <RowDefinition Height="48"/>
                <!--客户区-->
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!--标题区域-->
            <Grid Grid.Row="0">
                <TextBlock x:Name="TitleText" Text="CasseiaCoreX" VerticalAlignment="Center" FontWeight="Bold" Margin="20,0,0,0"/>
            </Grid>

            <!--导航视图-->
            <NavigationView x:Name="MainNavView" Grid.Row="1" IsSettingsVisible="False" ItemInvoked="MainNavView_ItemInvoked">
                <NavigationView.MenuItems>
                    <NavigationViewItem x:Name="NavigateHome" Content="主页" Icon="Home"/>
                    <NavigationViewItem x:Name="NavigateSysSettings" Content="系统设置" Icon="Setting"/>
                    <NavigationViewItem x:Name="NavigateTools" Content="常用工具" Icon="Library"/>
                    <NavigationViewItem x:Name="NavigateADB" Content="安卓调试">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xEE64;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Name="NavigateAbout" Content="关于">
                        <NavigationViewItem.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE783;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    
                </NavigationView.MenuItems>
                <Frame x:Name="MainFrame"/>
            </NavigationView>

        </Grid>
        
        <Grid x:Name="AwaitOverlay" Visibility="Collapsed">
            <Grid.Background>
                <AcrylicBrush TintColor="Black" TintOpacity="0.95"/>
            </Grid.Background>
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Vertical" Spacing="20">
                    <ProgressRing IsActive="True" Width="80" Height="80" Foreground="White"/>
                    <TextBlock x:Name="AwaitText" Text="请等待后台操作或弹出窗口返回" TextAlignment="Center" Foreground="White" FontSize="20" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
