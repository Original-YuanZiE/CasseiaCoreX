<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CasseiaCoreX.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CasseiaCoreX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CasseiaCoreX">

    <Grid x:Name="MainGrid">
        
        <Grid.RowDefinitions>
            <!--标题栏-->
            <RowDefinition Height="48"/>
            <!--客户区-->
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!--标题区域-->
        <Grid Grid.Row="0">
            <TextBlock x:Name="TitleText" Text="CasseiaCoreX Beta" VerticalAlignment="Center" FontWeight="Bold" Margin="20,0,0,0"/>
        </Grid>

        <!--导航视图-->
        <NavigationView x:Name="MainNavView" Grid.Row="1" IsSettingsVisible="False" ItemInvoked="MainNavView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="NavigateHome" Content="主页" Icon="Home"/>
                <NavigationViewItem x:Name="NavigateSysSettings" Content="系统设置" Icon="Setting"/>
                <NavigationViewItem x:Name="NavigateTools" Content="常用工具" Icon="Library"/>
            </NavigationView.MenuItems>
            <Frame x:Name="MainFrame"/>
        </NavigationView>
        
    </Grid>
    
</Window>
